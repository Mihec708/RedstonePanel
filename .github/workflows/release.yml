name: Release
on:
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  probe:
    strategy:
      matrix:
        - name: Ubuntu
          os: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "matrix without fail-fast OK / ${{ matrix.name }}"
