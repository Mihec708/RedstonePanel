name: Release
on:
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  probe:
    strategy:
      fail-fast: false
      matrix:
        - name: Ubuntu
          os: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "static runs-on with matrix OK / ${{ matrix.name }}"
