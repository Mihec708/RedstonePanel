name: Release
on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
jobs:
  build-and-release:
    strategy:
      fail-fast: false
      matrix:
        - name: Ubuntu
          os: ubuntu-22.04
    runs-on: ${{ matrix.os }}
    steps:
      - name: Probe
        run: echo "ubuntu matrix probe OK — ${{ matrix.name }}"
