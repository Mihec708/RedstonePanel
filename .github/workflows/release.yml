name: Release
on:
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "release file probe OK on ${{ github.ref_name }}"
